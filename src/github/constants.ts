const endpoints = {
  USERS: 'https://api.github.com/users',
};

const notFoundMessages = {
  USER: 'Usuário não encontrado',
  REPO: 'Repositório não encontrado',
};

const userTypes = {
  USER: 'User',
  ORGANIZATION: 'Organization',
};

export { endpoints, notFoundMessages, userTypes };
